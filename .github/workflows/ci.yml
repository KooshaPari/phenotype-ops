name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  fmt:
    name: Format (cargo fmt --check)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - uses: Swatinem/rust-cache@82a92a6e8fbeee089604da2575dc567ae9ddeaab
        with:
          workspaces: tools/phenotype-manifest -> tools/phenotype-manifest/target
      - name: cargo fmt --check
        run: cargo fmt --manifest-path tools/phenotype-manifest/Cargo.toml --all -- --check

  clippy:
    name: Lint (cargo clippy)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@82a92a6e8fbeee089604da2575dc567ae9ddeaab
        with:
          workspaces: tools/phenotype-manifest -> tools/phenotype-manifest/target
      - name: cargo clippy
        run: cargo clippy --manifest-path tools/phenotype-manifest/Cargo.toml --all-targets --all-features -- -D warnings

  test:
    name: Test (cargo test)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@82a92a6e8fbeee089604da2575dc567ae9ddeaab
        with:
          workspaces: tools/phenotype-manifest -> tools/phenotype-manifest/target
      - name: cargo test
        run: cargo test --manifest-path tools/phenotype-manifest/Cargo.toml --all-targets --all-features
